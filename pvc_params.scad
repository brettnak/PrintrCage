// Half inch pvc params

pipe_diameter = 21.4;
pipe_radius   = pipe_diameter / 2.0;

pipe_tolerance = 0.6;
pipe_slot_diameter = pipe_diameter + pipe_tolerance;
pipe_slot_radius = pipe_slot_diameter / 2.0;
pipe_tolerance_radial = pipe_tolerance / 2.0;
